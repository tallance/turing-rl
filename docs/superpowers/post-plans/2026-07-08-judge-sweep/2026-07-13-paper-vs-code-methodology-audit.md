# Paper vs. Code Methodology Audit — Turing-RL (SFT + judge)

**Paper:** *Learning User Simulators with Turing Rewards*, arXiv **2606.19336** (cs.CL, LaTeXML build dated 2026-06-17).
**Access:** Full text accessed on 2026-07-13. The `WebFetch`/`WebSearch` tools are blocked (internet mode off), but `curl` from bash reached arXiv directly — I read the abstract page and the full HTML v1 (`https://arxiv.org/html/2606.19336v1`), including §2–3 (method/SFT/reward), Appendix B (sampling), Appendix C (SFT/GRPO tables), and Appendix E (judge prompts). Citations below point to the paper's own section/table/appendix labels. Paper content is quoted only where the paper states it; anything the paper does not state is marked "not specified in paper."

## Comparison table

Columns: **what we did** · **original code** (as-imported upstream = git commit `6aaecfb`, verified via `git show`) · **current repo** (with our edits) · **paper**. "Match" compares the *current code* to the paper.

| # | Point | What we did/expected | Original code (`6aaecfb`) | Current repo (our edits) | Paper says (cite) | Match? |
|---|-------|----------------------|---------------------------|--------------------------|-------------------|--------|
| 1 | Generation decoding | upstream defaults, no overrides in `heldout_inference.sh` | max_tokens 2048, top_p 1.0, top_k -1, pres 0.5, rep 1.0, temp 0.4/0.6 | **unchanged** | Table 4 (App. B.1): vLLM, max 2048, temp 0.4 Reddit/0.6 Chat, top_p 1.0, top_k -1, pres 0.5, rep 1.0 | ✅ exact (orig = cur = paper) |
| 2 | SFT packing + attention | retrained `--no_packing` | **`packing=True` default + `attn_implementation="sdpa"` hardcoded** (→ cross-contamination); `flash_attn` pinned but unused | we pass `--no_packing` (deviates from orig default) | not specified; states completion-only loss w/ prompt/history **masked** (App. C.1) | ❓ paper silent (our choice upholds the paper's masking) |
| 3 | Reasoning format + SFT loss | kept upstream | `<reasoning>…</reasoning>\n[HUMAN]: y`; `completion_only_loss=True`, assistant-span mask | **unchanged** | §2.3/App. C.1: identical target, completion-only loss, prompt/history masked | ✅ exact (orig = cur = paper) |
| 4 | Thinking mode (Qwen `<think>`) | thinking OFF everywhere | SFT/infer `enable_thinking=False`; **CoT traces generated by a *thinking* OpenRouter model** (`generate_cot.py`) | unchanged (our served CoT client also forces `enable_thinking=False`) | §3.3, Tables 5–6, App. C.1: base "Qwen3-8B (thinking disabled)"; trace gen thinking-off | ✅ (SFT/infer thinking-off) |
| 5 | LoRA / SFT hyperparameters | corrected yaml to Table 5 | **yaml `lora_r=16, lora_alpha=32, use_qlora=true`** (no save-cadence keys) | r64/α128/bf16, save_steps=10, + `--max_seq_length 8192` via script | Table 5: r/α 64/128, dropout 0.05, LR 2e-4 cosine, seq 8192, batch 1×16, eff 128, 3 ep, bf16 | ⚠️ **orig code ≠ paper (r16/qlora); our D5 fix = paper** ✅ |
| 6 | Judge / reward-model decoding | froze to served `generation_config.json`; no wire override | `_openai_chat`: no sampling unless env set; `JUDGE_MODEL=qwen/qwen3.5-397b-a17b` | unchanged (we set env at sweep time) | §3.2: reward judge = Qwen3.5-397B-A17B; **judge decoding not specified** | ⚠️ model ✅, decoding unspecified |
| 7 | Pairwise Turing reward | kept upstream | `_score_pairwise_likert_with_info`; `(min{s,5}−1)/6` **+ 0.9 scale + format bonuses + per-response penalties + length penalty** (all present in `6aaecfb`) | **unchanged** | §2.2/§3.4.1: pairwise, position-randomized, 1–7 Likert × 3 criteria, `r=(min{s,5}−1)/6`, length penalty (App. C.3) | ✅ core + rubric; ⚠️ the extras (0.9 scale/bonuses/penalties) are **original-code additions beyond the paper**, not ours |
| 8 | Response parsing | kept parse + added residual-reasoning strip | `parse_reasoning_and_response`, `_split_on_human_tag`, `HUMAN_PREFIX="[HUMAN]:"`; **no pair-builder / no residue-strip** | `scripts/build_judge_pairs.py` (new) adds `_strip_reasoning_residue` | §2.3/App. B.2: `[HUMAN]:` marker after `<reasoning>` specified; exact mechanics not | ✅ (mechanics are impl detail) |

**Original-code notes** (from `git show 6aaecfb`): points **1, 3, 6, 7** are byte-for-byte unchanged by us; points **2, 5, 8** are where our current repo diverges from the as-imported code. Two are notable: (a) **point 5 — the original released code's yaml (`r16/α32/QLoRA`) itself disagreed with the paper's own Table 5** (`r64/α128/bf16`); our D5 change fixed the code to match the paper, i.e. it corrected an upstream code-vs-paper bug rather than deviating from the paper. (b) **point 7 — the reward "extras" (0.9 scale, format bonuses, per-response penalties) are original upstream code**, present in the first commit, so they are inherited, not introduced by us; only the paper's plain `(min{s,5}−1)/6` + length penalty is described in the text.

## Decision impact

- **Point 1 (generation decoding):** Paper **supports keeping our current choice unchanged** — our constants are byte-for-byte Table 4 (max 2048, top_p 1.0, top_k -1, presence 0.5, rep 1.0, temp 0.4/0.6). The observed repetition-loop degeneration and 2048-cap truncations are inherent to the paper's own published config, not a deviation on our part; any fix would be a deliberate departure from the paper.
- **Point 2 (packing):** Paper is **silent on packing/attention**, so it neither mandates nor forbids `--no_packing`; there is no methodology to "match" here. Keeping `--no_packing` is a safe implementation choice — it *upholds* the one thing the paper does state (completion-only loss with prompt/history masked, App. C.1), which trl `packing=True` under sdpa can violate via cross-conversation attention leakage. Recommend keeping it.

## Conflicts

No hard conflicts found. Every point either matches the paper exactly (1, 3, 4, 5, 7-core, 8) or concerns something the paper does not specify (2 packing; 6 judge decoding). The only soft divergences are additive, not contradictory: point 7's reward pipeline adds a 0.9 raw-reward scale, format bonuses, and per-response penalties on top of the paper's `(min{s,5}−1)/6` (the paper does include a length penalty). Minor note: `qwen3_8b_lora.yaml` does not itself set `max_seq_length` (code default 5120); the 8192 in Table 5 is supplied by the SFT launch scripts via `--max_seq_length 8192`.
