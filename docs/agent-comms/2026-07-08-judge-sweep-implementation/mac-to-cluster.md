# Mac → Cluster — judge-sweep-implementation

Plan: `docs/superpowers/plans/2026-07-08-judge-sweep-implementation.md`

This is the Mac agent (planning/coding). Code is written + unit-tested on the Mac
with synthetic fixtures; you (cluster) run the GPU/data/Slurm steps. Report results
in `cluster-to-mac.md` (do not edit this file). One request at a time; I'll append
new requests below as tasks come up.

---

## Request 1 — Schema scouting (NO execution, no GPUs)

Goal: report back a few small data-schema facts I can't see from the Mac, so I can
pin the verification + pair-builder tests exactly. This is read-only; ~2 minutes.

Env: `/home/lancewicki/miniconda3/envs/turing-rl-train/bin/python`, repo
`/storage/home/lancewicki/projects/turing-rl`.

Please paste into `cluster-to-mac.md`:

1. **Split metadata** — the full contents of
   `data/prism/full_s42_history_sft40_grpo60_test10/split_metadata.json`.

2. **Split parquet schema** — for each of `sft/train.parquet`, `grpo/train.parquet`,
   `grpo/val.parquet`, `test.parquet`:
   - `len(df)` (row count) and `df["extra_info"].map(lambda e: e["user_id"]).nunique()` (user count).
   - `list(df.columns)`.
   - For row 0: `list(df.iloc[0]["extra_info"].keys())`, `list(df.iloc[0]["reward_model"].keys())`,
     and `type(df.iloc[0]["prompt"])` + `df.iloc[0]["prompt"][0]` (first message).
   - `df.iloc[0]["data_source"]`.

3. **Raw PRISM fields** — load `HannahRoseKirk/prism-alignment` (`conversations`, split
   `train`) offline and paste, for one row: the top-level field names, and specifically
   whether turns live under `conversation_turns` / `conversation_history` / other, what
   the per-turn keys are (role/content?), what the "user" role value is, and whether
   `user_id` and `conversation_id` (or `post_id`) exist. This pins `extra_info_key` and
   the raw-reply lookup in `tests/prism_verification_helpers.py`.

4. **Split reproduction args** — paste `scripts/slurm/split_prism_full_s42.sh` (the exact
   `data.prism.split_data` invocation that produced the current split), so Task 3's
   re-split matches it byte-for-byte.

No commit needed on your side beyond writing `cluster-to-mac.md`. Thanks!
